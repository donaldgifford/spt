// Package search declares the Search interface for the listing index and
// its Meilisearch implementation. The api role exposes search to both the
// UI (via the generated OpenAPI client) and external API consumers.
package search
