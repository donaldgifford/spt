// Package agent adapts the agentic framework (sdk-booty-sh per ADR-0011)
// to spt's stage handlers — currently extract and judge. The Agent
// interface is the seam between the orchestrator and the LLM-touching code.
package agent
